spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sajo?currentSchema=user_account
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: false
