spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/sajo?currentSchema=user_account
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: false

# 로컬 전용 값 - 실제 계정 데이터가 없는 로컬 DB에서만 쓰임, 운영 키와 무관
sajo:
  crypto:
    account-key: local-dev-account-encryption-key
    account-salt: e9f26d0f9854767f
    account-hash-key: reM647WLMdaO/aIbzmDTKhOnJN0dcy00pcU2OKa4bq4=
  jwt:
    secret: user-service-test-only-jwt-secret-value-not-used-anywhere-else
    access-token-validity-seconds: 3600
