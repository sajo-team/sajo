spring:
  application:
    name: auth-service
  config:
    import: "configserver:http://localhost:8888"

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka