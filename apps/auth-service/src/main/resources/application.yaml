spring:
  application:
    name: auth-service
  config:
    import: "configserver:http://config-server:8888"

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka