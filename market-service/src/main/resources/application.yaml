server:
  port: 8082

spring:
  application:
    name: market-service
  datasource:
    url: jdbc:postgresql://localhost:5433/market_strategy_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update