server:
  port: 8083

spring:
  application:
    name: trading-service
  datasource:
    url: jdbc:postgresql://localhost:5434/trading_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update