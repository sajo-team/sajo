spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sajo?currentSchema=trading
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: false
