server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sajo-team/sajo-config-repo.git
          username: hyeok0008
          password: ${GITHUB_TOKEN}